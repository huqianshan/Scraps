#include "helper.h"
using namespace std;

int main(int argc, char const *argv[])
{

    int m;
    int n;
    string ret;
    while (cin >> m >> n)
    {
        // ret = cal_arr(m, n);
        cout << ret << endl;
    }

    //

    /* code */
    return 0;
}
